html
	head
		style
			include style.css
	body
		h1 Result
		br
		p Yay! we found a match: 
		br 
		ul
			each thing in result
				li #{thing}
				br
		a(href='addUser') Add user
		br
		br
		a(href='form') Search again
		br
		br
		a(href='users') Show all users in the database


			//- This didn't work because, I was looping through an object, whereas the object type is an array. 
			//- each thing in result
			//- 	li #{thing.firstName + ' ' + thing.lastName + ' ' + thing.emailadres}		